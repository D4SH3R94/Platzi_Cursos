// While 
/* while (condition) {
    // código a ser executado enquanto a condição for verdadeira
}  */ 
let contador = 0;

while (contador < 15) {
    console.log("Contador é: " + contador);
    contador++;
}